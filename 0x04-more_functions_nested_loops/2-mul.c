#include "main.h"

/**
 * mul - takes two integers and multiplies them.
 * @a: integer to multiply.
 * @b: integer to multiply.
 *
 * Return: product of n and x.
 */

int mul(int a, int b)
{
	return (a * b);
}
